@model IEnumerable<User_School>
@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Parents List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Parent" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp; Invite Parent
            </a>
        </div>
    </div>
    <br />
    <br />

    <table class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>

                <th>
                    Parent Name
                </th>
                <th>
                    Parent Email
                </th>
                <th>
                    Phone Number
                </th>



            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>


                    <td width="30%">
                        @obj.ApplicationUser.FullName
                    </td>
                    <td width="30%">
                        @obj.ApplicationUser.Email
                    </td>
                    <td width="30%">
                        @obj.ApplicationUser.PhoneNumber
                    </td>


                    <td width="30%">
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="Parent" asp-action="Edit" asp-route-id="@obj.ApplicationUser.Id" class="btn btn-primary mx-2">
                                <i class="bi bi-pencil-square"></i>
                                Edit
                            </a>

                            <a asp-controller="Parent" asp-action="Delete" asp-route-id="@obj.ApplicationUser.Id" class="btn btn-danger mx-2">
                                <i class="bi bi-trash"></i>
                                Delete
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>